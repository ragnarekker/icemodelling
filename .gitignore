# Workspace config for PyCharm
.idea/
'.idea'

# pyc files (compilations of the .py files..)
*.pyc

# .pickles are used for local storage in this repo
*.pickle

# log files are made when executing the code. Not repo material
*.log

# windows .ini filer
*.ini

# these are the local configurations needed to run the project.
# When missing, the files with the leading underscore are used as default
api.json
folders.json

# Removing other content of private files when adding to git
plotsforisvarsling_1h.py
plotsforisvarsling_24h.py
_setenvironment.py